{
  "name": "monorepo-dev",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "kong",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "postCreateCommand": "git submodule update --init --recursive && echo \"$AUTH_SERVICE_SECRETS\" > ./services/auth/.env && docker compose up -d",
  "remoteEnv": {
    "AUTH_SERVICE_SECRETS": "default_value_if_needed"
  }
}
